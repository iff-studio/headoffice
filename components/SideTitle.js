export default function SideTitle ({ children }) {
    return <span className="whitespace-nowrap lg:block hidden absolute top-0 right-full mr-4 pt-2 border-t-2 border-transparent">{children}</span>
}
